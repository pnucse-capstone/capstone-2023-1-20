using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EditEvent : MonoBehaviour
{
    /*
    public float bpm = 1;
    public float offset = 1;
    public int beats = 1;
    void Update()
    {
        float sperbeat = 60F / bpm;
        for(int i=0;i<8*beats; i++)
        {
            var pos = NotePosition.defaultFunction(Game.time-Game.time%sperbeat+i * sperbeat/beats +offset);
            Debug.DrawLine(pos,pos+Vector3.up,Color.red);
        }
    }
    public void BPMAdapt()
    {
        Game.table.adaptEventBPM(bpm,offset,beats);
    }
    public void NoteBPMAdapt()
    {
        Game.table.adaptNoteBPM(bpm, offset, beats);
    }
    */
}
