using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraBackground : MonoBehaviour
{
    /*
    Vector3 prev;
    Material mat;
    // Start is called before the first frame update
    [SerializeField]
    SelectCameraMove cam;
    void Start()
    {
        prev = Camera.main.transform.position;
        mat = GetComponent<SpriteRenderer>().material;
    }
    [SerializeField]
    Color[] Colors;
    // Update is called once per frame
    void Update()
    {
        Vector3 now = Camera.main.transform.position;
        now.z = -1f;
        gameObject.transform.position = now/1.5F;
        prev = Camera.main.transform.position;

        if (cam.isFocusEnding())
        {
            mat.SetColor("_Color",Colors[1]);
        }
        else if (Hardmode.isHardmode)
        {
            mat.SetColor("_Color", Colors[2]);
        }
        else
        {
            mat.SetColor("_Color", Colors[0]);
        }

    }*/
}
